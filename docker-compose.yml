version: '3.8'

services:
  profit-pilot-ai:
    build: .
    container_name: profit-pilot-ai
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - ./data:/app/data
